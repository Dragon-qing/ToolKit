add_library(ui_lib)

target_sources(ui_lib PRIVATE
    base/basewidget.cpp
    base/basewidget.ui

    components/basetable.cpp
    components/basetable.ui

    components/unionplot.cpp
    components/unionplot.ui

    components/dlgprompt.cpp
    components/dlgprompt.ui

    components/flowlayout.cpp

    widgetmanager.cpp
    widgetmanager.ui
)

target_include_directories(ui_lib PUBLIC 
    ${CMAKE_CURRENT_SOURCE_DIR}
    base
    components
)
target_link_libraries(ui_lib PUBLIC 
    Qt${QT_VERSION_MAJOR}::Widgets 
    Qt${QT_VERSION_MAJOR}::Core
    utils_lib
    core_lib
    qhotkey
    modules_lib
    thirdparty_lib
)